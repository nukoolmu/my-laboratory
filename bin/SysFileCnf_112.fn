SysFileCnf_112(){
    RESULT=$(grep "[[:space:]]/tmp[[:space:]]" /etc/fstab | grep nodev)

    if [ -z "${RESULT}" ];then
        result "FAIL" "1.1.2" "Set Nodev Option for /tmp Partiton"
    else
        result "PASS" "1.1.2" "Set Nodev Option for /tmp Partiton"
    fi

}