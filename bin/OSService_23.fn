OSService_23(){

    RESULT=$(rpm -q rsh-server |grep 'is not installed')

    if [ -z "${RESULT}" ];then
        AUDIT_RESULT='FAIL'
        CUR_RESULT='The Package had installed'
    else
        AUDIT_RESULT='PASS'
        CUR_RESULT='The package does not installed'
    fi

    result "$AUDIT_RESULT" "2.3" "Remove rsh-server"
    writeFile "$AUDIT_RESULT" "2.3" "Remove rsh-server" '=' "Package rsh-server be remove" "$CUR_RESULT"

}
