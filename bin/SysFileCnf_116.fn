SysFileCnf_116(){
    RESULT=$(grep -e "^/tmp[[:space:]]" /etc/fstab | grep /var/tmp)

    if [ -z "${RESULT}" ];then
        result "FAIL" "1.1.6" "Bind Mount the /var/tmp directory to tmp"
    else
        result "PASS" "1.1.6" "Bind Mount the /var/tmp directory to tmp"
    fi

}