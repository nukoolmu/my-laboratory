SysFileCnf_114(){
    RESULT=$(grep "[[:space:]]/tmp[[:space:]]" /etc/fstab | grep noexec)

    if [ -z "${RESULT}" ];then
        result "FAIL" "1.1.4" "Set noexec option for /tmp Partition"
    else
        result "PASS" "1.1.4" "Set noexec option for /tmp Partition"
    fi

}